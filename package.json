{
  "name": "agentx-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo dev --filter=agentx",
    "build": "turbo build",
    "build:prod": "turbo build:prod",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.4.2"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild"]
  }
}
